node_modules/
coverage/
.DS_Store
*.db
*.db.new
*.db-shm
*.db-wal
*.log
tests/manual/data/

# Synthetic CSV/TSV fixture build artifact (explicit for readability;
# already covered by the global *.db pattern above). The CSV/TSV in source/
# are tracked (CC0).
tests/fixtures/synthetic-csv/synthetic-csv.db
